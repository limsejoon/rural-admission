# PRD — Rural Admission Wiki (농어촌 자격자 대학 지원 플랫폼)

Version: v1.1 (서울대 검증 반영)
Status: 설계 확정 · 1교 구현 완료
Platform: GitHub Pages + AI Pack + 5-Layer 데이터 시스템

---

## 1. 프로젝트 개요

**Rural Admission Wiki** — 농어촌 자격자가 지원 가능한 모든 대학 지원 경로를 구조화하고,
사용자가 웹 또는 AI를 통해 자기 상황에 맞게 탐색하는 시스템.

이 프로젝트는 모집인원 숫자 저장 프로젝트가 아니다.
**숫자가 왜 나왔는지(근거)와 대학별 전형 구조를 저장하는 프로젝트다.**

대상: 인서울 약 25개교 + 지방 거점/국립대 약 10개교 (≈35). 기준: **2028학년도**.

> ⚠️ 2028학년도 주의: 내신 5등급제·통합형 수능 전환 첫 해. 비교할 과거 데이터가 거의 없어
> "근거 저장" 원칙이 특히 중요. 기존 9등급제 기반 예시(예: "내신 2.4")는 재해석 필요.

---

## 2. 해결하려는 문제

- **PDF 분산** — 모집인원·면접·수능최저가 수십 페이지에 흩어짐
- **전형명 혼란** — 기회균형/고른기회/사회통합/농어촌특별 등 이름이 제각각이나 모두 농어촌 지원 경로일 수 있음
- **매년 반복** — 인원·군·학과가 매년 바뀜. 기존엔 매번 처음부터 재작성

목표 흐름: `PDF 추가 → 변경 감지 → 업데이트` (처음부터 재작성 X)

---

## 3. 사용자

- **사용자 A (라이트)** — 바로 확인·비교. 예: "서울대 정시 농어촌 몇 명?"
- **사용자 B (AI 활용)** — AI Pack 업로드 후 자기 상황 질문. 예: "면접 없는 대학만", "내 점수로 가능한 곳". *(파일 업로드 되는 유료 AI 플랜 대상)*

---

## 4. 사용자 경험

- **첫 화면** — 3초 안에 이해. [📖 그냥 보기] / [🤖 AI와 함께 보기]
- **그냥 보기** — 설치 없이: 모집인원·수시/정시·수능최저·면접·PDF 원문
- **AI와 함께 보기** — AI Pack(ZIP) 다운로드 → ChatGPT/Claude/Gemini에 업로드 → 질문
- **메인 노출** — 대학명·수시·정시·PDF. **가/나/다군은 숨김**(질문 시만 제공)

---

## 5. 데이터 구조 — 5개 레이어

HTML에 숫자를 직접 쓰지 않는다. 모든 숫자는 JSON에서 렌더링.

| 레이어 | 폴더 | 내용 | 노출 |
|---|---|---|---|
| L1 Raw | `data/raw_data/<year>/` | 원본 PDF | AI Pack |
| L2 Extracted | `data/extracted/` | 단과대학·학과별 원본 분해 | AI Pack 전용 |
| L3 Structure | `data/structures/` | 전형명·면접·수능최저·농어촌 유형 (잘 안 바뀜) | 웹 |
| L4 Yearly | `data/yearly/<year>/` | 모집인원·군 (매년) | 웹 |
| L5 Evidence | `data/evidence/` | 근거(페이지·산출방식·검수상태) | 웹/AI |

`track_id`로 L3·L4·L5 연결.

---

## 6. 핵심 규칙

### 농어촌 유형 (rural_type)
- `standalone` (농어촌 단독) — 농어촌만 별도 선발 → `[농어촌 단독]`
- `included` (농어촌 포함) — 기회균형 등에 포함 → `[농어촌 포함]`

### 농어촌 인원 상태 (rural_quota_status) — v1.1 신규
실제 PDF 검증으로 도출. 같은 "포함형"도 두 갈래:
- `specified` — 농어촌 인원이 별도 명시됨 → 그 숫자 사용 (예: 서울대 정시 농어촌 87명)
- `merged` — 다른 대상과 합산되어 농어촌 단독 미상 → 전체 숫자만, 농어촌 null (예: 서울대 수시 사회통합 183명)
- `pending` — 아직 추출 안 함

### 표시 규칙 — 배지가 의미를 바꾼다
- `specified`/`standalone` → "이 숫자가 농어촌 몫"
- `merged`/`included` → "전형 전체 인원이며, 농어촌이 몇 명인지는 자료에 없음"

### 추정 금지 (최우선)
- ❌ "아마 가능"  →  ⭕ "현재 자료에서 확인되지 않음"
- PDF에 없는 숫자는 만들지 않는다. 웹·AI Pack·콘솔 모두 적용.

### granularity
- 웹: 전형 총원까지. 학과별 질문은 AI가 L2를 참조해 답하되, 없으면 "확인되지 않음"
  (예: 서울대 정시 농어촌은 공과대학 17명까지만 명시, 컴공 학과별은 정원 10% 상한 규칙만 존재)

---

## 7. 추출·검수 파이프라인

```
PDF → 파서(MinerU/PyMuPDF4LLM) → 스키마·규칙대로 구조화 → 검수 콘솔로 보정 → 확정 JSON → git push → GitHub Pages
```
- 추출은 관리자가 로컬/Actions에서 사전 수행 (정적 호스팅).
- **검수 콘솔**(`tools/review-console.html`): 좌 PDF 대조 / 우 필드 편집 / `AI 추출` 버튼(해마다 재사용) / 단과대학 합 검산 / 4개 레이어 내보내기.

## 8. 업데이트 전략

새 PDF(`raw_data/2029/`) → 콘솔 `AI 추출` → structure 비교 → 변경 감지 → evidence 갱신 → `yearly/2029/` 생성 → 배포.
structure는 재사용, 숫자만 보정.

---

## 9. 구현 현황

- ✅ 데이터 스키마 3종
- ✅ 서울대 1교 — 5개 레이어 실제 데이터 + 교차검증
- ✅ 검수 콘솔(AI 추출 에이전트 내장)
- ✅ 웹 렌더링(랜딩·카드·상세·AI 안내)
- ⬜ data/ fetch 연결 / 나머지 34교 / AI Pack 빌더 / 배포

자세한 상태·TODO는 `PROGRESS.md`.
